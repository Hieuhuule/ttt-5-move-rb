#!/usr/bin/env ruby

require_relative '../lib/move.rb'


# welcome the player by outputting a 
# friendly message to the terminal: "Welcome to Tic Tac Toe!".

puts "Welcome to Tic Tac Toe!"

# establish the starting state of the game, i.e. the empty board. 
# Create a new board by setting a variable board equal to instantiating 
# a new array with 9 elements, each of which is a blank space, " ".

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

# ask the user for input by outputting "Where would you like to go?" 
puts "Where would you like to go?"

# Use gets.strip to store their input to a variable, input.

input = gets.strip

# convert the user's input to an array index using our 
# #input_to_index method and store this as the variable index.

index = input_to_index(input)

# call our #move method. Do so with the arguments of the board, 
# the index the user wants to access and the default player of "X".

move(board, index)

# display the board by calling the #display_board method, passing in 
# the necessary arguments required to run this method.

display_board(board)